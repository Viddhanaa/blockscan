# Viddhana Contracts Environment Configuration
# Copy this file to .env and fill in your values

# Private key of the contract owner/deployer (64 hex characters, without 0x prefix)
# WARNING: Never commit your actual private key to version control!
# Generate using: openssl rand -hex 32
OWNER_PRIVATE_KEY=REPLACE_WITH_PRIVATE_KEY_NEVER_COMMIT

# RPC URL for the Viddhana network
RPC_URL=http://localhost:8545

# Chain ID for Viddhana network (must match genesis.json)
CHAIN_ID=47777
